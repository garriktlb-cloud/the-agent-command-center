
Change `.slice(0, 4)` to `.slice(0, 3)` in `src/pages/Coaching.tsx`, and update the Continue learning grid from `md:grid-cols-2` to `md:grid-cols-3` so the 3 tiles sit in a single row on desktop and stack on mobile.

### Files
- `src/pages/Coaching.tsx` — `slice(0, 3)` and `grid-cols-1 md:grid-cols-3`
